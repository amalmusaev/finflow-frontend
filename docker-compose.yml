services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finflow-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - finflow-network

networks:
  finflow-network:
    external: true
